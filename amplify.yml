version: 1
frontend:
  phases:
    preBuild:
      commands:
        - node -v
        - npm -v
        - npm ci
        - npx prisma generate
       
    build:
      commands:
        - env | grep -e DATABASE_URL -e AUTH_SECRET -e MVP_SELLER_ID -e S3_BUCKET -e KAKAO_REST_API_KEY -e KAKAO_CLIENT_SECRET -e NAVER_CLIENT_ID -e NAVER_CLIENT_SECRET >> .env
        - env | grep -e NEXT_PUBLIC_ >> .env || true
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
